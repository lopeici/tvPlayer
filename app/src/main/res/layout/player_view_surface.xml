<?xml version="1.0" encoding="utf-8"?>
<!--
  PlayerView with a SurfaceView surface — used on real devices: cheaper composition (no extra
  GPU copy pass like TextureView), fewer dropped frames on weak TV chipsets, HDR passthrough.
  The emulator keeps the TextureView variant (player_view.xml); SurfaceView overlays often
  render green/garbled there.
-->
<androidx.media3.ui.PlayerView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:focusable="false"
    android:focusableInTouchMode="false"
    android:descendantFocusability="blocksDescendants"
    app:surface_type="surface_view"
    app:use_controller="false"
    app:resize_mode="fit"
    app:keep_content_on_player_reset="true"
    app:shutter_background_color="@android:color/black" />