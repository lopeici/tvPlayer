*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/
# Signing — never commit
keystore.properties
*.jks
*.keystore
# Personal build seed (contains IPTV credentials) — never commit
personal.properties
# Kotlin build cache
.kotlin/
